location /guide/ {
    root /var/www;
    index index.html;
    try_files $uri $uri/ =404;
}

location ~ ^/guide/.+\.(png|jpe?g|gif|webp|svg)$ {
    root /var/www;
    expires 30d;
    add_header Cache-Control "public, immutable";
    access_log off;
}
